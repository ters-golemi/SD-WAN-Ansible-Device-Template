# Device-specific variables for template attachment
# Update these values based on your environment

# Template Information (from Exercise 1)
template_name: "vSmart_Template"
template_id: ""  # Optional: Provide template ID if known

# Device Information (from Exercise 2)
device_uuid: "00000000-0000-0000-0000-000000000001"
device_hostname: "vsmart-1"
device_system_ip: "1.1.1.1"

# Device-specific template variables
# These variables depend on your specific device template configuration
# Common vSmart template variables include:

device_vars:
  # System variables
  system_host_name: "vsmart-1"
  system_system_ip: "1.1.1.1"
  system_site_id: "100"
  
  # Optional: Add other template variables as required by your template
  # Examples:
  # system_latitude: "37.7749"
  # system_longitude: "-122.4194"
  # system_device_groups: "vsmart-group"
  # vpn0_gateway: "10.0.0.1"
  # vpn0_ip_address: "10.0.1.10/24"
  
# Note: The exact variables required depend on your device template configuration
# Use the SD-WAN Manager GUI to identify required variables for your template
